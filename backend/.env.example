PORT=5000
NODE_ENV=development
FRONTEND_ORIGIN=http://localhost:5173

# MySQL connection (use your current library_system database)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=library_system

# Borrow/return rules
LOAN_DAYS=14
FINE_PER_DAY=1000

# JWT
JWT_SECRET=change_me_to_a_long_random_secret
